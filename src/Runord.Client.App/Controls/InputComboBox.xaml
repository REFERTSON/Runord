<UserControl x:Class="Runord.Client.App.Controls.CustomComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             MinWidth="200" Width="300" Height="40">
    <Border Style="{StaticResource CustomComboBoxOuterBorderStyle}">
        <Grid Margin="0,0,-2,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Кнопка, которая отображает выбранное значение или Placeholder -->
            <ToggleButton x:Name="PART_DropDownButton"
                          Grid.Column="0"
                          Background="Transparent"
                          BorderThickness="0"
                          HorizontalContentAlignment="Left"
                          VerticalContentAlignment="Center"
                          Padding="12,8,35,8"
                          Click="PART_DropDownButton_Click"
                          Focusable="False">
                <ToggleButton.Style>
                    <Style TargetType="ToggleButton">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="Transparent" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ToggleButton.Style>
                <TextBlock x:Name="PART_DisplayText"
                           Text="{Binding Placeholder, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Foreground="{StaticResource CustomComboBoxPlaceholderBrush}"
                           FontSize="14"
                           VerticalAlignment="Center"/>
            </ToggleButton>

            <!-- Стрелка -->
            <ToggleButton x:Name="PART_ArrowButton"
                          Grid.Column="1"
                          Width="35"
                          Background="Transparent"
                          BorderThickness="0"
                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                          Click="PART_ArrowButton_Click"
                          Focusable="False">
                <ToggleButton.Template>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="templateRoot" Background="Transparent" Width="35">
                            <Path x:Name="arrow"
                                  Data="M0,0 L4,4 8,0 z"
                                  Fill="{StaticResource CustomComboBoxArrowBrush}"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  RenderTransformOrigin="0.5,0.5">
                                <Path.RenderTransform>
                                    <RotateTransform Angle="0"/>
                                </Path.RenderTransform>
                            </Path>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="arrow"
                                                             Storyboard.TargetProperty="(Path.RenderTransform).(RotateTransform.Angle)"
                                                             To="180" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="arrow"
                                                             Storyboard.TargetProperty="(Path.RenderTransform).(RotateTransform.Angle)"
                                                             To="0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </ToggleButton.Template>
            </ToggleButton>

            <!-- Popup -->
            <Popup x:Name="PART_Popup"
                   AllowsTransparency="True"
                   Placement="Bottom"
                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   PopupAnimation="Slide"
                   VerticalOffset="1">
                <Border Background="{StaticResource CustomComboBoxPopupBackgroundBrush}"
                        BorderBrush="{StaticResource CustomComboBoxPopupBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="4"
                        Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        MaxHeight="200">
                    <ScrollViewer x:Name="PART_ScrollViewer"
                                  HorizontalScrollBarVisibility="Disabled"
                                  VerticalScrollBarVisibility="Hidden">
                        <StackPanel x:Name="PART_ItemsPanel" />
                    </ScrollViewer>
                </Border>
            </Popup>
        </Grid>
    </Border>
</UserControl>