<UserControl x:Class="Runord.Client.Views.Windows.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <Grid>
        <StackPanel Width="300" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Вход в Runord" FontSize="24" FontWeight="Bold" Margin="0,0,0,30" HorizontalAlignment="Center"/>
            <TextBlock Text="Email" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Email}" Margin="0,0,0,15"/>
            <TextBlock Text="Пароль" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,20"/>
            <Button Content="Войти" Command="{Binding LoginCommand}" Width="100" Height="30" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" TextWrapping="Wrap"/>
            <Button Content="Регистрация" Command="{Binding NavigateToRegisterCommand}" Background="Transparent" BorderThickness="0" Foreground="Gray" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>